# malem — Cloudflare Pages + Functions + D1 config.
name = "malem"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"

# Created with:  wrangler d1 create malem-db
# Paste the printed database_id below, then apply schema.sql.
[[d1_databases]]
binding = "DB"
database_name = "malem-db"
database_id = ""
